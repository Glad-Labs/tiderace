# OPTIONAL — only for `--llm anthropic`.
#
# The default extraction backend is local Ollama, whose client is plain urllib,
# so tiderace has no Python dependencies at all out of the box.
anthropic>=1.0
